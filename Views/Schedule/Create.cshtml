@model Schedule
@{ ViewData["Title"] = "New schedule"; }
<div class="form-shell"><div class="page-heading"><div><div class="eyebrow">New service</div><h2>Add a train schedule</h2><p>Create a reusable timetable for future booking records.</p></div></div><form asp-action="Create" method="post" class="surface-card"><partial name="_ScheduleForm" model="Model" /><div class="form-actions"><a asp-action="Index" class="btn btn-outline-secondary">Cancel</a><button class="btn btn-brand"><i class="bi bi-check2 me-1"></i>Save schedule</button></div></form></div>
